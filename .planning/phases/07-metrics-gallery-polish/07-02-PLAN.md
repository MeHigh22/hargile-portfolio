---
phase: 07-metrics-gallery-polish
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/case-study/CaseStudyPanel.tsx
autonomous: false
requirements: [CSCONT-04, CSCONT-05, CSCONT-06, CSCONT-07, CSNAV-03, CSNAV-04]

must_haves:
  truths:
    - "Metrics section shows animated counters instead of placeholder bordered cards"
    - "Gallery section shows masonry image grid instead of placeholder text list"
    - "Testimonial blockquote has upgraded visual weight (larger text, more prominent)"
    - "Team cards are polished with consistent styling"
    - "Reading progress bar appears at top of case study on scroll"
    - "Next project card appears at bottom and navigates to next case study"
  artifacts:
    - path: "src/components/case-study/CaseStudyPanel.tsx"
      provides: "Fully wired case study with all Phase 7 components"
      contains: "MetricCounter"
  key_links:
    - from: "src/components/case-study/CaseStudyPanel.tsx"
      to: "src/components/case-study/MetricCounter.tsx"
      via: "import and render in Resultats section"
      pattern: "import.*MetricCounter"
    - from: "src/components/case-study/CaseStudyPanel.tsx"
      to: "src/components/case-study/DeliverableGallery.tsx"
      via: "import and render after Livrables title"
      pattern: "import.*DeliverableGallery"
    - from: "src/components/case-study/CaseStudyPanel.tsx"
      to: "src/components/case-study/ReadingProgressBar.tsx"
      via: "import and render inside panel div, receives panelRef"
      pattern: "ReadingProgressBar.*panelRef"
    - from: "src/components/case-study/CaseStudyPanel.tsx"
      to: "src/components/case-study/NextProjectCard.tsx"
      via: "import and render at bottom, receives projectId"
      pattern: "NextProjectCard.*projectId"
---

<objective>
Wire all Phase 7 components into CaseStudyPanel: replace metric and gallery placeholders, upgrade testimonial and team styling, add reading progress bar and next project card.

Purpose: Complete the bottom half of the case study experience — users see compelling results, browse deliverables, read testimonials, and flow to the next project.
Output: Updated CaseStudyPanel.tsx with all 6 requirements fulfilled.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-metrics-gallery-polish/07-01-SUMMARY.md

@src/components/case-study/CaseStudyPanel.tsx
@src/data/types.ts

<interfaces>
<!-- Components created in Plan 01 — import and use directly -->

From src/components/case-study/MetricCounter.tsx:
```typescript
interface MetricCounterProps {
  metric: CaseStudyMetric;
  index: number;
  panelRef: React.RefObject<HTMLDivElement | null>;
  reducedMotion: boolean;
}
export function MetricCounter({ metric, index, panelRef, reducedMotion }: MetricCounterProps): JSX.Element;
```

From src/components/case-study/DeliverableGallery.tsx:
```typescript
interface DeliverableGalleryProps {
  images: string[];
  panelRef: React.RefObject<HTMLDivElement | null>;
  reducedMotion: boolean;
}
export function DeliverableGallery({ images, panelRef, reducedMotion }: DeliverableGalleryProps): JSX.Element;
```

From src/components/case-study/ReadingProgressBar.tsx:
```typescript
interface ReadingProgressBarProps {
  panelRef: React.RefObject<HTMLDivElement | null>;
}
export function ReadingProgressBar({ panelRef }: ReadingProgressBarProps): JSX.Element;
```

From src/components/case-study/NextProjectCard.tsx:
```typescript
interface NextProjectCardProps {
  projectId: string;
}
export function NextProjectCard({ projectId }: NextProjectCardProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire all components into CaseStudyPanel and upgrade testimonial/team styling</name>
  <files>src/components/case-study/CaseStudyPanel.tsx</files>
  <action>
    Import the 4 new components at the top of CaseStudyPanel.tsx:
    ```
    import { MetricCounter } from './MetricCounter';
    import { DeliverableGallery } from './DeliverableGallery';
    import { ReadingProgressBar } from './ReadingProgressBar';
    import { NextProjectCard } from './NextProjectCard';
    ```

    **1. ReadingProgressBar (CSNAV-03):**
    Add `<ReadingProgressBar panelRef={panelRef} />` as the first child inside the panel div (before BackButton). It renders fixed, so DOM position doesn't affect layout.

    **2. Resultats section (CSCONT-04):**
    Replace the placeholder bordered grid with MetricCounter components:
    ```tsx
    <CaseStudySection title="Resultats">
      <div className="grid grid-cols-2 md:grid-cols-3 gap-x-8 gap-y-6">
        {caseStudy.metrics.map((metric, i) => (
          <MetricCounter
            key={metric.label}
            metric={metric}
            index={i}
            panelRef={panelRef}
            reducedMotion={isReducedMotion}
          />
        ))}
      </div>
    </CaseStudySection>
    ```
    Note: No borders, no card wrappers (per user decision — editorial, not dashboard).

    **3. Livrables section (CSCONT-05):**
    Keep the CaseStudySection title wrapper for the "Livrables" heading. Replace the placeholder `<ul>` list with DeliverableGallery OUTSIDE the CaseStudySection (gallery needs full-width breakout, CaseStudySection constrains to max-w-800px):
    ```tsx
    <CaseStudySection title="Livrables">
      <p className="text-text-secondary text-sm mb-2">
        {caseStudy.deliverables.length} livrables principaux
      </p>
    </CaseStudySection>
    {/* Gallery breaks out of prose column */}
    {caseStudy.galleryImages && caseStudy.galleryImages.length > 0 && (
      <DeliverableGallery
        images={caseStudy.galleryImages}
        panelRef={panelRef}
        reducedMotion={isReducedMotion}
      />
    )}
    ```
    The deliverables text list can optionally remain inside the CaseStudySection as a brief summary, or be removed. Claude's discretion — keep if it adds context, remove if redundant with the gallery.

    **4. Temoignage section (CSCONT-06):**
    Upgrade the existing blockquote for "prominent pull-out" visual weight per user decision:
    - Increase border-l thickness: `border-l-2` -> `border-l-4`
    - Increase quote text size: `text-lg` -> `text-xl md:text-2xl`
    - Add more padding-left: `pl-6` -> `pl-8`
    - Add top/bottom padding for breathing room: `py-6`
    - Keep italic, keep accent border color
    - Author line stays the same (font-mono, text-sm)

    **5. Equipe section (CSCONT-07):**
    Polish the existing team cards per user decision (keep bordered grid, small cards):
    - Add a subtle accent-colored initial/avatar circle before the name: a `w-10 h-10 rounded-full bg-accent/10 flex items-center justify-center text-accent font-bold text-sm` div showing the first letter of the name
    - Keep the existing border, rounded-lg, padding pattern
    - Ensure consistent spacing

    **6. NextProjectCard (CSNAV-04):**
    Add after the last CaseStudySection, before the bottom padding div:
    ```tsx
    <NextProjectCard projectId={projectId} />
    ```
    Remove or reduce the bottom padding div (`<div className="h-16" />`) since the NextProjectCard provides visual closure.

    **7. Verify overflow-x:**
    Ensure the panel div has `overflow-x-hidden` in addition to `overflow-y-auto` to prevent full-width gallery/card from causing horizontal scroll. Update className if needed: `"fixed inset-0 z-[100] overflow-y-auto overflow-x-hidden bg-bg"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/case-study/__tests__/</automated>
  </verify>
  <done>
    - CaseStudyPanel imports and renders all 4 new components
    - Resultats section uses MetricCounter (no borders, editorial style)
    - Livrables section uses DeliverableGallery with full-width breakout
    - Testimonial blockquote has upgraded visual weight (border-l-4, text-xl)
    - Team cards have avatar initials and polished layout
    - ReadingProgressBar at top of panel
    - NextProjectCard at bottom of panel
    - No horizontal scrollbar (overflow-x-hidden)
    - TypeScript compiles clean
    - All tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 7 case study experience</name>
  <files>src/components/case-study/CaseStudyPanel.tsx</files>
  <action>
    Verify all Phase 7 components are working correctly in the browser:
    1. Run `npm run dev` and open http://localhost:5174
    2. Click on any project slide to open its case study
    3. Scroll through and verify all 6 requirements visually
    4. Test next-project circular navigation through all 3 projects
    5. Test reduced motion path
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>User has visually confirmed all Phase 7 features work correctly</done>
  <what-built>Complete Phase 7: animated metric counters, masonry deliverables gallery, upgraded testimonial pull-out, polished team credits, reading progress bar, and next project navigation card — all wired into the case study panel.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5174
    2. Click on any project slide to open its case study
    3. Scroll down through the case study and verify:
       a. **Progress bar**: thin accent-colored bar appears at very top of screen after first scroll, fills left to right as you scroll
       b. **Resultats section**: metric numbers count up as you scroll into view (not bordered cards — just big number + label). Try scrolling past and back — counters should NOT reset
       c. **Livrables section**: full-width masonry gallery with staggered image reveals. 2 columns on narrow, 3 on wide viewport. No horizontal scrollbar
       d. **Temoignage section**: blockquote with thick accent left border, large italic text — visually prominent
       e. **Equipe section**: team member cards with initial circles and name/role
       f. **Next project card**: full-width card at bottom showing next project hero, title, category, "Projet suivant" label
    4. Click the next project card — should directly swap to the next case study (no return to slider)
    5. Navigate through all 3 projects via next-project cards to verify circular loop (atlas -> pulse -> verde -> atlas)
    6. Test reduced motion: enable prefers-reduced-motion in DevTools, reopen a case study — counters should show final values immediately, gallery images should be visible without animation
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
npx tsc --noEmit
npx vitest run
Open http://localhost:5174, test all 3 case studies end-to-end
</verification>

<success_criteria>
- All 6 Phase 7 requirements visually verified in browser
- Metric counters animate once and stay
- Gallery masonry layout works on mobile and desktop widths
- Progress bar tracks scroll accurately
- Next project navigation loops circularly through all 3 projects
- Testimonial and team sections look polished
- No horizontal scrollbar in case study
- Reduced motion path works for all new components
- TypeScript clean, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-metrics-gallery-polish/07-02-SUMMARY.md`
</output>
