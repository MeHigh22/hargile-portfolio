---
phase: 07-metrics-gallery-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/types.ts
  - src/data/projects.ts
  - src/components/case-study/MetricCounter.tsx
  - src/components/case-study/DeliverableGallery.tsx
  - src/components/case-study/ReadingProgressBar.tsx
  - src/components/case-study/NextProjectCard.tsx
  - src/components/case-study/TestimonialBlock.tsx
  - src/components/case-study/TeamCredits.tsx
  - src/components/case-study/__tests__/MetricCounter.test.tsx
  - src/components/case-study/__tests__/DeliverableGallery.test.tsx
  - src/components/case-study/__tests__/ReadingProgressBar.test.tsx
  - src/components/case-study/__tests__/NextProjectCard.test.tsx
  - src/components/case-study/__tests__/TestimonialBlock.test.tsx
  - src/components/case-study/__tests__/TeamCredits.test.tsx
autonomous: true
requirements: [CSCONT-04, CSCONT-05, CSCONT-06, CSCONT-07, CSNAV-03, CSNAV-04]

must_haves:
  truths:
    - "User sees metric values count up as they scroll into the results section"
    - "User sees a full-width masonry image gallery when scrolling to deliverables"
    - "User sees a progress bar fill across the top of the screen as they scroll"
    - "User sees a next-project card at the bottom that navigates to the next case study"
    - "User sees a visually prominent testimonial pull-out quote with accent border"
    - "User sees polished team member cards with initial avatars"
  artifacts:
    - path: "src/components/case-study/MetricCounter.tsx"
      provides: "Animated counter card with parseMetricValue and ScrollTrigger one-shot"
      exports: ["MetricCounter"]
    - path: "src/components/case-study/DeliverableGallery.tsx"
      provides: "CSS columns masonry grid with ScrollTrigger stagger reveals"
      exports: ["DeliverableGallery"]
    - path: "src/components/case-study/ReadingProgressBar.tsx"
      provides: "Fixed top bar tracking panel scroll position"
      exports: ["ReadingProgressBar"]
    - path: "src/components/case-study/NextProjectCard.tsx"
      provides: "Full-width hero card for next project with circular navigation"
      exports: ["NextProjectCard"]
    - path: "src/components/case-study/TestimonialBlock.tsx"
      provides: "Prominent pull-out blockquote with accent border styling"
      exports: ["TestimonialBlock"]
    - path: "src/components/case-study/TeamCredits.tsx"
      provides: "Team member card grid with initial avatar circles"
      exports: ["TeamCredits"]
    - path: "src/data/types.ts"
      provides: "galleryImages field on CaseStudyContent"
      contains: "galleryImages"
    - path: "src/data/projects.ts"
      provides: "Unsplash gallery image URLs for all 3 projects"
      contains: "galleryImages"
  key_links:
    - from: "src/components/case-study/MetricCounter.tsx"
      to: "gsap ScrollTrigger"
      via: "ScrollTrigger.create with scroller: panelRef.current"
      pattern: "scroller.*panelRef"
    - from: "src/components/case-study/NextProjectCard.tsx"
      to: "src/store/useViewStore.ts"
      via: "useViewStore.getState().openCase(nextId)"
      pattern: "openCase"
---

<objective>
Create six standalone case study components (MetricCounter, DeliverableGallery, ReadingProgressBar, NextProjectCard, TestimonialBlock, TeamCredits) plus the data layer extension (galleryImages), each with unit tests.

Purpose: Build all Phase 7 building blocks as tested, self-contained components before wiring into CaseStudyPanel.
Output: 6 new components, 6 test files, updated types.ts and projects.ts with gallery image data.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-metrics-gallery-polish/07-RESEARCH.md

@src/data/types.ts
@src/data/projects.ts
@src/store/useViewStore.ts
@src/components/case-study/CaseStudyPanel.tsx
@src/components/case-study/__tests__/Timeline.test.tsx

<interfaces>
<!-- Key types and contracts from existing codebase -->

From src/data/types.ts:
```typescript
export interface CaseStudyMetric {
  label: string;
  value: string;
}

export interface CaseStudyTestimonial {
  quote: string;
  author: string;
  role: string;
}

export interface CaseStudyTeamMember {
  name: string;
  role: string;
}

export interface CaseStudyContent {
  challenge: string;
  solution: string;
  timeline: CaseStudyTimelineStep[];
  metrics: CaseStudyMetric[];
  deliverables: string[];
  testimonial: CaseStudyTestimonial;
  team: CaseStudyTeamMember[];
}

export interface ProjectData {
  id: string;
  title1: string;
  title2: string;
  category: string;
  heroImg: string;
  colors: ProjectColors;
  caseStudy?: CaseStudyContent;
  // ... other fields
}
```

From src/store/useViewStore.ts:
```typescript
type ViewMode = 'slider' | 'case';
interface ViewState {
  mode: ViewMode;
  activeProjectId: string | null;
  openCase: (projectId: string) => void;
  closeCase: () => void;
}
export const useViewStore = create<ViewState>(...);
```

From src/components/case-study/__tests__/Timeline.test.tsx (GSAP mock pattern):
```typescript
vi.mock('gsap', () => ({
  default: {
    context: vi.fn(() => ({ revert: vi.fn() })),
    to: vi.fn(),
    set: vi.fn(),
    registerPlugin: vi.fn(),
    matchMedia: vi.fn(() => ({ add: vi.fn(), revert: vi.fn() })),
  },
}));
vi.mock('gsap/ScrollTrigger', () => ({
  ScrollTrigger: { create: vi.fn(), refresh: vi.fn() },
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data layer extension and MetricCounter + DeliverableGallery components with tests</name>
  <files>
    src/data/types.ts,
    src/data/projects.ts,
    src/components/case-study/MetricCounter.tsx,
    src/components/case-study/DeliverableGallery.tsx,
    src/components/case-study/__tests__/MetricCounter.test.tsx,
    src/components/case-study/__tests__/DeliverableGallery.test.tsx
  </files>
  <behavior>
    MetricCounter:
    - Renders metric.value as-is when reducedMotion=true
    - Renders metric.label text
    - Handles value strings with prefix/suffix ("+140%", "-60%", "4.7/5", "3x secteur")
    - Has no border or card wrapper (editorial style: number + label only)

    DeliverableGallery:
    - Renders one img per galleryImages URL
    - Each image wrapper has break-inside-avoid class for CSS columns
    - Uses columns-2 md:columns-3 layout classes
    - Images use lazy loading attribute
  </behavior>
  <action>
    1. Add `galleryImages?: string[]` to CaseStudyContent in src/data/types.ts (parallel to existing `deliverables: string[]`, not replacing it).

    2. Add galleryImages arrays to all 3 projects in src/data/projects.ts. Use 6 Unsplash URLs per project with tech/design themes:
       - Atlas: data analytics/dashboard themed (`?fm=webp&w=800`)
       - Pulse: health/wellness app themed
       - Verde: eco/sustainability themed
       Pattern: `https://images.unsplash.com/photo-{id}?fm=webp&w=800&q=80`

    3. Write tests first, then create MetricCounter.tsx:
       - `parseMetricValue(value: string)` utility function (can be in same file, exported for testing):
         - Regex: `^([^0-9-]*)(-?[\d.]+)(.*)$`
         - Returns `{ prefix, numeric, suffix }`
         - Fallback: `{ prefix: '', numeric: 0, suffix: value }` if no match
       - Component props: `{ metric: CaseStudyMetric; index: number; panelRef: React.RefObject<HTMLDivElement | null>; reducedMotion: boolean }`
       - Reduced motion: render `metric.value` directly, no GSAP setup
       - Full motion: gsap.context scoped to element, ScrollTrigger.create with `scroller: panelRef.current`, `start: 'top 85%'`, `onEnter` fires gsap.to with `snap: { val: hasDecimal ? 0.1 : 1 }`, duration 1s, ease power2.out, delay `index * 0.12`
       - No borders, no card wrappers. Just: large number (text-4xl md:text-5xl, font-bold, text-accent) + label below (font-mono, text-xs, text-text-secondary, uppercase, tracking-wider)
       - Use GSAP mock pattern from Timeline.test.tsx

    4. Write tests first, then create DeliverableGallery.tsx:
       - Props: `{ images: string[]; panelRef: React.RefObject<HTMLDivElement | null>; reducedMotion: boolean }`
       - Full-width breakout: `w-screen relative left-1/2 -translate-x-1/2` wrapper
       - Inner masonry: `columns-2 md:columns-3 gap-4 px-4 md:px-8`
       - Each image in `break-inside-avoid mb-4` div with ref for GSAP targeting
       - Images: `<img src={url} alt="" loading="lazy" className="w-full h-auto block rounded-lg" />`
       - Full motion: useEffect with gsap.context, gsap.set each image `{ opacity: 0, y: 24 }`, ScrollTrigger.create per image with `scroller: panelRef.current`, `start: 'top 88%'`, `onEnter` fires gsap.to with `delay: i * 0.08`, duration 0.5s, ease power3.out
       - Reduced motion: no gsap setup, images visible by default
  </action>
  <verify>
    <automated>npx vitest run src/components/case-study/__tests__/MetricCounter.test.tsx src/components/case-study/__tests__/DeliverableGallery.test.tsx</automated>
  </verify>
  <done>
    - parseMetricValue correctly parses all metric value formats in projects.ts
    - MetricCounter renders value and label; reduced motion shows final value
    - DeliverableGallery renders images in CSS columns layout with lazy loading
    - galleryImages field added to types and populated for all 3 projects
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReadingProgressBar and NextProjectCard components with tests</name>
  <files>
    src/components/case-study/ReadingProgressBar.tsx,
    src/components/case-study/NextProjectCard.tsx,
    src/components/case-study/__tests__/ReadingProgressBar.test.tsx,
    src/components/case-study/__tests__/NextProjectCard.test.tsx
  </files>
  <behavior>
    ReadingProgressBar:
    - Renders with initial opacity 0 (hidden until first scroll)
    - Has fixed position at top of screen
    - Uses accent color (bg-accent)
    - Height is 3px (h-[3px])
    - z-index above case study (z-[110])

    NextProjectCard:
    - Renders next project's title and category
    - Shows "Projet suivant" label
    - Circular: after last project (verde), wraps to first (atlas)
    - Calls useViewStore.openCase on click
  </behavior>
  <action>
    1. Write tests first, then create ReadingProgressBar.tsx:
       - Props: `{ panelRef: React.RefObject<HTMLDivElement | null> }`
       - Renders a fixed div: `fixed top-0 left-0 h-[3px] bg-accent z-[110]`
       - Initial inline style: `{ opacity: 0, width: '0%' }`
       - useEffect attaches scroll listener to `panelRef.current` (NOT window):
         - Computes `scrollTop / (scrollHeight - clientHeight) * 100`
         - First scroll past 10px: `gsap.to(barRef, { opacity: 1, duration: 0.3, ease: 'power2.out' })` (fade in)
         - Every scroll: `gsap.set(barRef, { width: pct + '%' })` (no React state, direct DOM)
         - Use `{ passive: true }` on the listener
       - Cleanup: remove scroll listener on unmount
       - Use hasFadedIn ref to prevent repeated fade-in tweens

    2. Write tests first, then create NextProjectCard.tsx:
       - Props: `{ projectId: string }` (current project ID)
       - Import `projects` from data, find current index, compute next: `(index + 1) % projects.length`
       - Full-width breakout wrapper: `w-screen relative left-1/2 -translate-x-1/2`
       - Layout: full-width card with next project's hero image as background, overlay gradient blending from current project accent toward next project accent
       - Content overlay: "Projet suivant" label (font-mono, text-xs, uppercase, tracking-wider), next project title (text-3xl md:text-4xl, font-bold), category
       - Background image: `<img>` with object-cover, absolute fill, same Unsplash pattern
       - Gradient overlay: linear-gradient from current accent (50% opacity) to next accent (70% opacity)
       - Click handler: `useViewStore.getState().openCase(nextProject.id)`
       - Hover state (Claude's discretion): subtle scale transform on the image, opacity shift on overlay
       - Min-height: `min-h-[300px] md:min-h-[400px]`
       - Cursor pointer on hover
       - Use GSAP mock and useViewStore mock in tests

    Mock useViewStore in NextProjectCard test:
    ```typescript
    vi.mock('../../store/useViewStore', () => ({
      useViewStore: {
        getState: vi.fn(() => ({ openCase: vi.fn() })),
      },
    }));
    ```
  </action>
  <verify>
    <automated>npx vitest run src/components/case-study/__tests__/ReadingProgressBar.test.tsx src/components/case-study/__tests__/NextProjectCard.test.tsx</automated>
  </verify>
  <done>
    - ReadingProgressBar renders fixed bar with opacity 0, correct z-index and accent color
    - NextProjectCard renders next project title, "Projet suivant" label, and wraps circularly
    - NextProjectCard click calls openCase with correct next project ID
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TestimonialBlock and TeamCredits components with tests</name>
  <files>
    src/components/case-study/TestimonialBlock.tsx,
    src/components/case-study/TeamCredits.tsx,
    src/components/case-study/__tests__/TestimonialBlock.test.tsx,
    src/components/case-study/__tests__/TeamCredits.test.tsx
  </files>
  <behavior>
    TestimonialBlock:
    - Renders the quote text in a blockquote element
    - Renders author name and role
    - Has accent-colored left border (border-l-4 border-accent)
    - Quote text is large and italic (text-xl md:text-2xl italic)
    - Has generous padding (pl-8 py-6)

    TeamCredits:
    - Renders one card per team member
    - Each card shows an initial avatar circle (first letter of name, bg-accent/10, text-accent)
    - Each card shows name and role
    - Uses a responsive grid layout (grid-cols-2 md:grid-cols-3)
    - Cards have border and rounded corners
  </behavior>
  <action>
    1. Write tests first, then create TestimonialBlock.tsx:
       - Props: `{ testimonial: CaseStudyTestimonial }`
       - Renders a `<blockquote>` with `border-l-4 border-accent pl-8 py-6`
       - Quote text: `text-xl md:text-2xl italic text-text-primary leading-relaxed`
       - Author line below quote: `mt-4 font-mono text-sm text-text-secondary not-italic`
         Format: "-- {author}, {role}"
       - No extra card wrapper or background -- pure editorial pull-out style
       - This extracts the testimonial markup currently inline in CaseStudyPanel into a standalone component

    2. Write tests first, then create TeamCredits.tsx:
       - Props: `{ team: CaseStudyTeamMember[] }`
       - Outer wrapper: `grid grid-cols-2 md:grid-cols-3 gap-4`
       - Each member card: `border border-border rounded-lg p-4 flex items-center gap-3`
       - Initial avatar circle: `w-10 h-10 rounded-full bg-accent/10 flex items-center justify-center text-accent font-bold text-sm` showing `member.name.charAt(0).toUpperCase()`
       - Name: `font-medium text-text-primary`
       - Role: `text-sm text-text-secondary`
       - This extracts the team card markup currently inline in CaseStudyPanel into a standalone component

    Test structure for both: use @testing-library/react render + screen queries.
    No GSAP mocking needed -- these are pure presentational components with no animation logic.
  </action>
  <verify>
    <automated>npx vitest run src/components/case-study/__tests__/TestimonialBlock.test.tsx src/components/case-study/__tests__/TeamCredits.test.tsx</automated>
  </verify>
  <done>
    - TestimonialBlock renders blockquote with accent border, large italic text, author attribution
    - TeamCredits renders grid of member cards with initial avatars, names, and roles
    - Both components are standalone with typed props matching existing data interfaces
    - All tests pass
  </done>
</task>

</tasks>

<verification>
npx vitest run src/components/case-study/__tests__/
npx tsc --noEmit
</verification>

<success_criteria>
- 6 new components exist and export correctly
- All component tests pass (6 test files)
- types.ts has galleryImages field
- projects.ts has 6 Unsplash URLs per project in galleryImages
- TypeScript compiles clean
- No new runtime dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/07-metrics-gallery-polish/07-01-SUMMARY.md`
</output>
