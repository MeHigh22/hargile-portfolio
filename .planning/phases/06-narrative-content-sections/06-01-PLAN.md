---
phase: 06-narrative-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/case-study/ProseBody.tsx
  - src/components/case-study/Timeline.tsx
  - src/components/case-study/__tests__/ProseBody.test.tsx
  - src/components/case-study/__tests__/Timeline.test.tsx
autonomous: true
requirements: [CSCONT-01, CSCONT-02, CSCONT-03]

must_haves:
  truths:
    - "ProseBody splits text on \\n\\n into multiple <p> elements"
    - "First paragraph renders with text-lg class, subsequent with text-base"
    - "All paragraphs use text-text color (primary) for long-form readability"
    - "Timeline renders one node per CaseStudyTimelineStep with phase name, duration, and description"
    - "Timeline has a vertical accent-colored connector line with circular nodes"
    - "Timeline accepts panelRef and reducedMotion props for GSAP ScrollTrigger setup"
    - "In reduced motion mode, Timeline renders fully visible with no GSAP"
  artifacts:
    - path: "src/components/case-study/ProseBody.tsx"
      provides: "Paragraph splitting with editorial typography"
      exports: ["ProseBody"]
    - path: "src/components/case-study/Timeline.tsx"
      provides: "Vertical timeline with scroll-triggered node reveals"
      exports: ["Timeline"]
    - path: "src/components/case-study/__tests__/ProseBody.test.tsx"
      provides: "Unit tests for ProseBody"
    - path: "src/components/case-study/__tests__/Timeline.test.tsx"
      provides: "Unit tests for Timeline"
  key_links:
    - from: "src/components/case-study/ProseBody.tsx"
      to: "text.split('\\n\\n')"
      via: "string split"
      pattern: "split.*\\\\n\\\\n"
    - from: "src/components/case-study/Timeline.tsx"
      to: "gsap ScrollTrigger"
      via: "per-node ScrollTrigger with scroller: panelRef.current"
      pattern: "ScrollTrigger\\.create"
---

<objective>
Create ProseBody and Timeline components as standalone building blocks for case study content.

Purpose: These components implement the editorial prose presentation (CSCONT-01, CSCONT-02) and animated process timeline (CSCONT-03) without modifying CaseStudyPanel. Plan 02 wires them in.
Output: Two new components with unit tests, ready for integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-narrative-content-sections/06-CONTEXT.md
@.planning/phases/06-narrative-content-sections/06-RESEARCH.md
@.planning/phases/05-scroll-infrastructure-view-scaffold/05-02-SUMMARY.md

<interfaces>
<!-- Types the executor needs. Extracted from src/data/types.ts -->

```typescript
export interface CaseStudyTimelineStep {
  phase: string;
  duration: string;
  description: string;
}
```

<!-- Existing hook API from src/hooks/useReducedMotion.ts -->

```typescript
export function useReducedMotion(): boolean;
```

<!-- CaseStudyPanel passes panelRef as React.RefObject<HTMLDivElement> -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProseBody component with editorial typography</name>
  <files>src/components/case-study/ProseBody.tsx, src/components/case-study/__tests__/ProseBody.test.tsx</files>
  <behavior>
    - Test: renders multiple <p> elements when text contains \n\n
    - Test: first <p> has class text-lg, subsequent <p>s have text-base
    - Test: all <p> elements have text-text class (primary color for readability)
    - Test: all <p> elements have leading-relaxed class
    - Test: empty paragraphs from consecutive \n\n are filtered out
    - Test: single paragraph text renders one <p> with text-lg
    - Test: wrapping div has space-y-6 class (~1.5rem gap)
  </behavior>
  <action>
    Create ProseBody.tsx:
    - Interface: `ProseBodyProps { text: string }`
    - Split text on `\n\n`, filter empty strings
    - Map to `<p>` elements: first gets `text-lg`, rest get `text-base`
    - All paragraphs get `leading-relaxed text-text` (primary color per user decision — NOT text-text-secondary)
    - Wrapper div with `space-y-6` for generous spacing
    - No pull quotes, drop caps, or decorative elements per user decision — clean editorial feel
    - No subtitle or intro line — section header only (handled by CaseStudySection wrapper)

    Create ProseBody.test.tsx:
    - Use @testing-library/react render + screen
    - Test paragraph splitting, class assignment, empty filtering
  </action>
  <verify>
    <automated>npx vitest run src/components/case-study/__tests__/ProseBody.test.tsx</automated>
  </verify>
  <done>ProseBody renders multi-paragraph text with editorial typography; all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Timeline component with vertical accent line and circular nodes</name>
  <files>src/components/case-study/Timeline.tsx, src/components/case-study/__tests__/Timeline.test.tsx</files>
  <behavior>
    - Test: renders one node per CaseStudyTimelineStep
    - Test: each node displays step.phase and step.duration
    - Test: each node displays step.description
    - Test: renders a vertical connector line element
    - Test: when reducedMotion=true, all nodes are visible (no opacity:0 initial state)
    - Test: component renders without errors when steps array is empty
  </behavior>
  <action>
    Create Timeline.tsx:
    - Interface: `TimelineProps { steps: CaseStudyTimelineStep[]; panelRef: React.RefObject<HTMLDivElement | null>; reducedMotion: boolean }`
    - Layout: relative container, vertical accent-colored line on the left (div with bg-accent, w-0.5), circular nodes at each step (w-4 h-4 rounded-full border-2 border-accent)
    - Each node row: phase name (font-mono text-sm text-accent) + duration (font-mono text-xs text-text-secondary) on same line, description (text-text text-sm) below
    - Connector line: div with `bg-accent`, `transform-origin: top`, initial `scaleY(0)` — GSAP animates to `scaleY(1)` via ScrollTrigger
    - Node dots: initial `backgroundColor` at accent/20 opacity (unfilled), GSAP tweens to solid accent (filled) on scroll
    - Nodes: initial `opacity: 0, y: 20` — GSAP reveals per-node with ScrollTrigger
    - All ScrollTrigger instances MUST use `scroller: panelRef.current` (never window — Phase 5 architecture decision)
    - Use plain useEffect (not useGSAP hook) with gsap.context() scoped to container ref
    - Context cleanup: expose cleanup via useImperativeHandle or return revert from effect. The parent CaseStudyPanel calls ctxRef.current?.revert() on close which handles panel-scoped context, but Timeline creates its own context. Use a ref + useEffect cleanup return to call ctx.revert().
    - Reduced motion path: if reducedMotion is true, render all nodes fully visible (filled dots, full opacity), no GSAP setup at all. Return early from useEffect.
    - Timeline stays within the 800px prose column (parent CaseStudySection handles max-w)
    - Connector line position: left-[7px] (centers on 16px node dot)
    - Node size: w-4 h-4 (16px)
    - Connector line width: w-0.5 (2px)

    Create Timeline.test.tsx:
    - Use @testing-library/react
    - Mock panelRef as { current: document.createElement('div') }
    - Test node rendering, text content, reduced motion path
    - Do NOT test GSAP animation behavior in unit tests (GSAP needs real DOM lifecycle)
  </action>
  <verify>
    <automated>npx vitest run src/components/case-study/__tests__/Timeline.test.tsx</automated>
  </verify>
  <done>Timeline renders vertical accent line with circular nodes showing phase/duration/description; reduced motion shows all nodes visible; all tests green</done>
</task>

</tasks>

<verification>
npx vitest run src/components/case-study/__tests__/ProseBody.test.tsx src/components/case-study/__tests__/Timeline.test.tsx
npx tsc --noEmit
</verification>

<success_criteria>
- ProseBody splits \n\n text into styled paragraphs with text-lg first / text-base rest
- Timeline renders CaseStudyTimelineStep[] as vertical accent-lined nodes
- Both components have passing unit tests
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-narrative-content-sections/06-01-SUMMARY.md`
</output>
