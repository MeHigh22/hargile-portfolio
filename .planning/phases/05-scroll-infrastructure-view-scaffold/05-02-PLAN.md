---
phase: 05-scroll-infrastructure-view-scaffold
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/case-study/CaseStudyPanel.tsx
  - src/components/case-study/CaseStudyHero.tsx
  - src/components/case-study/CaseStudySection.tsx
  - src/components/case-study/BackButton.tsx
  - src/components/layout/AppShell.tsx
  - src/hooks/useHashSync.ts
  - src/components/slider/Slider.tsx
autonomous: true
requirements: [CSNAV-01, CSNAV-02, CSVIS-03, CSVIS-04]

must_haves:
  truths:
    - "CaseStudyPanel renders as a full-screen overlay sibling of Slider in AppShell"
    - "Panel displays full-bleed hero with project title + category, followed by section placeholders in correct order"
    - "Back button is fixed top-left, says '← Retour' with backdrop blur, and triggers closeCase"
    - "Escape key triggers close from case study view"
    - "GSAP Observer is disabled when case study opens and re-enabled when it closes"
    - "Hash updates to #projectId/case-study on open and #projectId on close"
    - "Browser back from #atlas/case-study closes the panel instead of navigating slider"
    - "Layout is responsive: centered prose column on desktop, full-width with padding on mobile"
    - "Reduced motion users get opacity crossfade instead of spatial animations"
  artifacts:
    - path: "src/components/case-study/CaseStudyPanel.tsx"
      provides: "Full-screen overlay with section layout, entry/exit animations, gsap.context lifecycle, Escape key handler"
      min_lines: 80
    - path: "src/components/case-study/CaseStudyHero.tsx"
      provides: "Full-bleed hero image with project title + category overlaid, scroll parallax via ScrollTrigger"
      min_lines: 30
    - path: "src/components/case-study/CaseStudySection.tsx"
      provides: "Reusable section wrapper with data-anim='cs-section' attribute for GSAP targeting"
      min_lines: 15
    - path: "src/components/case-study/BackButton.tsx"
      provides: "Fixed top-left back button with backdrop blur"
      min_lines: 10
    - path: "src/components/layout/AppShell.tsx"
      provides: "CaseStudyPanel rendered as sibling of children (Slider)"
    - path: "src/hooks/useHashSync.ts"
      provides: "Extended popstate handler recognizing #projectId/case-study format"
    - path: "src/components/slider/Slider.tsx"
      provides: "Observer disable/enable subscription to useViewStore.mode"
  key_links:
    - from: "src/components/layout/AppShell.tsx"
      to: "src/components/case-study/CaseStudyPanel.tsx"
      via: "conditional render when useViewStore.activeProjectId is truthy"
      pattern: "activeProjectId.*CaseStudyPanel"
    - from: "src/components/case-study/CaseStudyPanel.tsx"
      to: "src/store/useViewStore.ts"
      via: "closeCase() called from BackButton, Escape key, and exit animation"
      pattern: "closeCase"
    - from: "src/components/slider/Slider.tsx"
      to: "src/store/useViewStore.ts"
      via: "subscribe to mode changes to disable/enable Observer"
      pattern: "useViewStore\\.subscribe"
    - from: "src/hooks/useHashSync.ts"
      to: "src/store/useViewStore.ts"
      via: "popstate handler checks for /case-study suffix and calls openCase or closeCase"
      pattern: "case-study"
---

<objective>
Build the CaseStudyPanel component with full layout skeleton, wire it into AppShell, extend hash routing, and toggle GSAP Observer on mode change.

Purpose: This is the structural core of Phase 5 -- the panel that users see when they click into a case study. It handles rendering, navigation (back button, Escape, browser back), Observer lifecycle, and responsive layout. Entry/exit animations and the slide CTA button are included here since they are tightly coupled to the panel lifecycle.

Output: Working case study overlay that can be opened/closed via useViewStore, with correct Observer toggling, hash routing, and responsive layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scroll-infrastructure-view-scaffold/05-CONTEXT.md
@.planning/phases/05-scroll-infrastructure-view-scaffold/05-RESEARCH.md
@.planning/phases/05-scroll-infrastructure-view-scaffold/05-01-SUMMARY.md

@src/components/layout/AppShell.tsx
@src/components/slider/Slider.tsx
@src/components/slider/Slide.tsx
@src/hooks/useHashSync.ts
@src/hooks/useReducedMotion.ts
@src/store/useSliderStore.ts
@src/store/useViewStore.ts
@src/data/types.ts
@src/data/projects.ts
@src/theme/theme-utils.ts
@src/theme/color-utils.ts

<interfaces>
From src/data/types.ts (after Plan 01):
```typescript
export interface CaseStudyContent {
  challenge: string;
  solution: string;
  timeline: CaseStudyTimelineStep[];
  metrics: CaseStudyMetric[];
  deliverables: string[];
  testimonial: CaseStudyTestimonial;
  team: CaseStudyTeamMember[];
}
export interface ProjectData {
  // ... existing fields ...
  caseStudy?: CaseStudyContent;
}
```

From src/store/useViewStore.ts (after Plan 01):
```typescript
export const useViewStore = create<ViewState>((set) => ({
  mode: 'slider',
  activeProjectId: null,
  openCase: (projectId: string) => void,
  closeCase: () => void,
}));
```

From src/store/useSliderStore.ts:
```typescript
export const useSliderStore = create<SliderState>((set, get) => ({
  currentIndex: number,
  direction: 1 | -1,
  isAnimating: boolean,
  totalSlides: number,
  goToSlide: (index: number) => void,
  nextSlide: () => void,
  prevSlide: () => void,
  setAnimating: (v: boolean) => void,
}));
```

From src/hooks/useReducedMotion.ts:
```typescript
export function useReducedMotion(): boolean;
```

From src/theme/color-utils.ts:
```typescript
export function deriveTheme(colors: ProjectColors): ThemePalette;
```

From src/theme/theme-utils.ts:
```typescript
export function applyTheme(palette: ThemePalette): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CaseStudyPanel layout components and BackButton</name>
  <files>src/components/case-study/CaseStudyPanel.tsx, src/components/case-study/CaseStudyHero.tsx, src/components/case-study/CaseStudySection.tsx, src/components/case-study/BackButton.tsx</files>
  <action>
    **BackButton.tsx** — Simple component:
    - Fixed position top-left (fixed top-6 left-6 z-[110])
    - Text: "← Retour" (French, per user decision)
    - Backdrop blur for glass-morphism: backdrop-blur-md bg-bg/40 rounded-full px-4 py-2
    - Accepts onClick prop, renders as a button element
    - Subtle hover state (e.g., bg-bg/60 transition-colors)

    **CaseStudySection.tsx** — Reusable section wrapper:
    - Accepts title (string), children (ReactNode), className (optional)
    - Renders a section element with data-anim="cs-section" attribute for GSAP targeting
    - Centered prose column layout: max-w-[800px] mx-auto px-6 md:px-0 py-16
    - Title rendered as h3 with font-mono text-sm uppercase tracking-widest text-accent mb-6

    **CaseStudyHero.tsx** — Full-bleed hero:
    - Accepts project (ProjectData) and panelRef (RefObject for ScrollTrigger scroller config)
    - Full viewport width, 60vh height (Claude's discretion per CONTEXT.md)
    - Hero image uses same picture/source/WebP pattern from Slide.tsx
    - Project title (title1 + title2) and category overlaid at bottom-left with text-shadow or gradient scrim
    - data-anim="cs-hero" attribute on the container
    - Expose heroImgRef via forwardRef or useRef for scroll parallax (Plan 03 will add ScrollTrigger)
    - overflow-hidden on container, scale-[1.05] on img (same parallax headroom pattern)

    **CaseStudyPanel.tsx** — The main overlay component:
    - Props: projectId (string)
    - Lookup project from projects array by id. If not found or no caseStudy data, render nothing.
    - Panel structure: fixed inset-0 z-[100] overflow-y-auto bg-bg
    - CSS initial state: opacity-0 invisible (CSS controls initial state, GSAP animates — per established pattern)
    - On mount, apply project's color theme: applyTheme(deriveTheme(project.colors)) — per CSVIS-02 requirement
    - Panel ref: useRef<HTMLDivElement>(null) — stored as panelRef, used as ScrollTrigger scroller
    - gsap.context() scoped to panelRef — ctxRef stores the context
    - Entry animation (two-beat curtain wipe per user decision):
      - Get slider container ref: document.querySelector('[data-slider-container]') (add data-slider-container to Slider.tsx container div)
      - Beat 1: slider fades + scales to 95% (~0.4s, power2.inOut)
      - Beat 2: panel opacity 1, pointer-events auto, then hero fades up, then sections stagger in with fade + translateY (~0.6s total, power3.out)
      - Reduced motion branch (isReducedMotion from useReducedMotion): simple opacity crossfade 0.3s, no spatial movement
      - onComplete: ScrollTrigger.refresh() (elements now in final position), set panel to visible/pointer-events auto if not already
    - Exit animation (closePanel function):
      - FIRST: ctxRef.current?.revert() — kills all ScrollTrigger instances (CRITICAL: before any animation)
      - Reverse of entry: panel fades out, slider scales back to 100% + fades in
      - Reduced motion: opacity crossfade 0.3s
      - onComplete of exit animation: useViewStore.getState().closeCase(), then Observer re-enables (handled by Slider.tsx subscription)
      - Reset panel scroll position to 0 before unmount
    - Escape key handler: useEffect that adds keydown listener for Escape, calls closePanel. Clean up on unmount.
    - Hash update: on mount push #projectId/case-study, closePanel pushes #projectId
    - Section layout renders (in order per user decision):
      - CaseStudyHero (full-bleed)
      - CaseStudySection title="Defi" — renders challenge prose
      - CaseStudySection title="Solution" — renders solution prose
      - CaseStudySection title="Processus" — placeholder div for timeline (Phase 6 fills it)
      - CaseStudySection title="Resultats" — placeholder div for metrics (Phase 7 fills it)
      - CaseStudySection title="Livrables" — placeholder div for gallery (Phase 7)
      - CaseStudySection title="Temoignage" — renders testimonial quote + author
      - CaseStudySection title="Equipe" — renders team members
    - BackButton at top level inside the panel, calls closePanel
    - Responsive: sections use CaseStudySection centered prose column, hero and gallery break full-width

    NOTE: Use direct DOM refs for GSAP (never string selectors). Use data-anim attributes. Follow established GSAP + useEffect pattern (no useGSAP hook).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 case study components compile. CaseStudyPanel has full section layout, entry/exit animation logic, Escape key handler, and gsap.context lifecycle. BackButton shows "← Retour" with backdrop blur. Hero displays full-bleed image with title overlay. Section wrapper provides consistent prose column.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AppShell, extend hash routing, toggle Observer, add Slide CTA</name>
  <files>src/components/layout/AppShell.tsx, src/hooks/useHashSync.ts, src/components/slider/Slider.tsx, src/components/slider/Slide.tsx</files>
  <action>
    **AppShell.tsx** — Add CaseStudyPanel as sibling of Slider:
    - Import useViewStore, CaseStudyPanel
    - Read activeProjectId from useViewStore
    - Conditionally render: {activeProjectId && <CaseStudyPanel projectId={activeProjectId} />}
    - Place AFTER {children} but BEFORE ContactCTA (z-index handles layering; panel at z-[100])
    - Do NOT nest inside Slider — must be a sibling (per architecture decision)

    **useHashSync.ts** — Extend popstate handler for case study format:
    - In the popstate handler, BEFORE existing slider logic:
      - Parse hash: const parts = hash.split('/')
      - If parts.length === 2 && parts[1] === 'case-study': call useViewStore.getState().openCase(parts[0]), return early
      - If current mode is 'case' and hash does not contain '/case-study': call useViewStore.getState().closeCase() — this handles browser back from case study
    - In the hash update effect (currentIndex changes): add guard — if useViewStore.getState().mode === 'case', skip hash update (don't overwrite case study hash with slider hash)
    - Import useViewStore at top

    **Slider.tsx** — Observer disable/enable + data attribute:
    - Add data-slider-container attribute to the container div (so CaseStudyPanel can find it for entry animation)
    - Store Observer in a ref: const obsRef = useRef<Observer | null>(null); then obsRef.current = obs inside the useEffect
    - Add a new useEffect that subscribes to useViewStore mode changes:
      ```
      useEffect(() => {
        const unsub = useViewStore.subscribe(
          (state) => state.mode,
          (mode) => {
            if (mode === 'case') obsRef.current?.disable();
            else obsRef.current?.enable();
          }
        );
        return () => unsub();
      }, []);
      ```
      NOTE: Zustand subscribe with selector requires zustand/subscribe middleware OR use getState pattern. Check existing zustand version — if v5, use subscribe with (state) => state.mode selector directly. If that API is not available, use: useViewStore.subscribe((state, prevState) => { if (state.mode !== prevState.mode) { ... } })
    - Do NOT kill the Observer on mode change — only disable/enable

    **Slide.tsx** — Add "Voir l'etude de cas" CTA button:
    - Import useSliderStore and useViewStore
    - Add button after the metadata tags div, inside the content panel relative z-10 wrapper
    - data-anim="cta" attribute for GSAP targeting
    - onClick: check useSliderStore.getState().isAnimating, if false call useViewStore.getState().openCase(project.id)
    - Styling: mt-6 font-mono text-sm tracking-wider text-accent border border-accent/40 px-4 py-2 rounded-full hover:bg-accent/10 transition-colors
    - Text: "Voir l'etude de cas →" (French per user decision)
    - Only render button if project.caseStudy exists (graceful for projects without case study data)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>AppShell renders CaseStudyPanel as Slider sibling. Hash routing handles #projectId/case-study format with correct popstate behavior. Observer disables on case study open, enables on close. Each slide with caseStudy data shows "Voir l'etude de cas" CTA. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero TypeScript errors
- `npx vitest run` — all tests pass (existing + new from Plan 01)
- Dev server: clicking CTA on a slide opens the case study panel with correct project content
- Dev server: "← Retour" button closes the panel and returns to slider
- Dev server: Escape key closes the panel
- Dev server: browser back from #atlas/case-study closes the panel
- Dev server: wheel/touch/keyboard do not affect slider while panel is open
- Dev server: panel layout is responsive (prose column centered on desktop, full-width on mobile)
</verification>

<success_criteria>
- CaseStudyPanel renders full-screen with all section areas in correct order
- Entry animation: two-beat curtain wipe (slider recedes, then panel reveals with stagger)
- Exit animation: reverse (panel fades out, slider returns)
- Reduced motion: opacity crossfade only
- Observer disabled while case study is open, re-enabled on close
- Hash updates correctly on open (#atlas/case-study) and close (#atlas)
- Browser back from case study closes panel, not slider
- All 3 projects accessible via CTA button on slides
- gsap.context().revert() called on every close path (button, Escape, popstate)
- No ghost ScrollTrigger instances after close/reopen cycles
</success_criteria>

<output>
After completion, create `.planning/phases/05-scroll-infrastructure-view-scaffold/05-02-SUMMARY.md`
</output>
