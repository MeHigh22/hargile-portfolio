---
phase: 05-scroll-infrastructure-view-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/types.ts
  - src/data/projects.ts
  - src/store/useViewStore.ts
  - src/store/__tests__/useViewStore.test.ts
  - src/data/__tests__/projects.test.ts
autonomous: true
requirements: [CSDATA-01, CSVIS-02]

must_haves:
  truths:
    - "CaseStudyContent type defines all required fields (challenge, solution, timeline, metrics, deliverables, testimonial, team)"
    - "All 3 projects (atlas, pulse, verde) have realistic French case study content with no lorem ipsum"
    - "useViewStore exposes mode ('slider' | 'case'), activeProjectId, openCase(), closeCase()"
    - "openCase is blocked while useSliderStore.isAnimating is true"
  artifacts:
    - path: "src/data/types.ts"
      provides: "CaseStudyContent, CaseStudyMetric, CaseStudyTimelineStep, CaseStudyTestimonial, CaseStudyTeamMember interfaces; ProjectData extended with caseStudy? field"
      contains: "CaseStudyContent"
    - path: "src/data/projects.ts"
      provides: "caseStudy property on all 3 projects with full French content"
      contains: "caseStudy"
    - path: "src/store/useViewStore.ts"
      provides: "Zustand store for view mode management"
      exports: ["useViewStore"]
    - path: "src/store/__tests__/useViewStore.test.ts"
      provides: "Unit tests for view store state transitions and isAnimating guard"
    - path: "src/data/__tests__/projects.test.ts"
      provides: "Tests verifying caseStudy data completeness for all 3 projects"
  key_links:
    - from: "src/store/useViewStore.ts"
      to: "src/store/useSliderStore.ts"
      via: "openCase checks useSliderStore.getState().isAnimating before setting mode"
      pattern: "useSliderStore\\.getState\\(\\)\\.isAnimating"
    - from: "src/data/projects.ts"
      to: "src/data/types.ts"
      via: "projects array typed as ProjectData with caseStudy field"
      pattern: "caseStudy:"
---

<objective>
Create the data layer and view state machine for the case study feature.

Purpose: Establishes the typed data model (CaseStudyContent), populates all 3 projects with rich French content, and creates the useViewStore Zustand store that coordinates slider/case-study mode switching. This is the foundation every subsequent plan depends on.

Output: Extended types, populated project data, useViewStore, and tests proving correctness.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scroll-infrastructure-view-scaffold/05-CONTEXT.md
@.planning/phases/05-scroll-infrastructure-view-scaffold/05-RESEARCH.md

@src/data/types.ts
@src/data/projects.ts
@src/store/useSliderStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define CaseStudyContent types and extend ProjectData</name>
  <files>src/data/types.ts, src/data/__tests__/projects.test.ts</files>
  <behavior>
    - Test: CaseStudyMetric has label (string) and value (string) fields
    - Test: CaseStudyTimelineStep has phase, duration, description fields
    - Test: CaseStudyTestimonial has quote, author, role fields
    - Test: CaseStudyTeamMember has name, role fields
    - Test: CaseStudyContent has challenge, solution (strings), timeline (array), metrics (array), deliverables (array), testimonial (object), team (array)
    - Test: ProjectData caseStudy field is optional (existing projects without it still compile)
  </behavior>
  <action>
    Add to src/data/types.ts the following interfaces exactly as specified in research:
    - CaseStudyMetric { label: string; value: string }
    - CaseStudyTimelineStep { phase: string; duration: string; description: string }
    - CaseStudyTestimonial { quote: string; author: string; role: string }
    - CaseStudyTeamMember { name: string; role: string }
    - CaseStudyContent { challenge: string; solution: string; timeline: CaseStudyTimelineStep[]; metrics: CaseStudyMetric[]; deliverables: string[]; testimonial: CaseStudyTestimonial; team: CaseStudyTeamMember[] }
    - Extend ProjectData with optional field: caseStudy?: CaseStudyContent

    Then create/extend src/data/__tests__/projects.test.ts to verify all 3 projects (atlas, pulse, verde) have caseStudy with all required fields populated and non-empty. Tests will initially fail (RED) since data is not yet added.
  </action>
  <verify>
    <automated>npx vitest run src/data/__tests__/projects.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All CaseStudy* types exported from types.ts. ProjectData has caseStudy? field. Tests exist but expect data from Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Populate case study data for all 3 projects and create useViewStore</name>
  <files>src/data/projects.ts, src/store/useViewStore.ts, src/store/__tests__/useViewStore.test.ts</files>
  <action>
    **Part A: Project data** — Add caseStudy property to atlas, pulse, and verde in projects.ts. All content in realistic French (no lorem ipsum). Per user decision:

    Each project gets:
    - challenge: 2-3 paragraphs explaining the client's problem
    - solution: 2-3 paragraphs describing Hargile's approach
    - timeline: 4 steps (e.g., Decouverte, Conception, Developpement, Lancement) with duration and description
    - metrics: 3-5 digital KPIs with concrete numbers (e.g., { label: "Trafic organique", value: "+140%" }, { label: "Temps de chargement", value: "-0.8s" })
    - deliverables: list of 4-6 strings describing what was delivered
    - testimonial: { quote: realistic French client quote, author: fictional French name, role: role title }
    - team: 3-4 members with fictional French names and role titles (e.g., "Marie Dupont", "Lead Designer")

    Atlas = SaaS analytics dashboard story. Pulse = health app story. Verde = eco marketplace story. Each narrative should be unique, specific to the project's domain, and read like real agency case study content.

    **Part B: useViewStore** — Create src/store/useViewStore.ts as a Zustand store:
    ```
    type ViewMode = 'slider' | 'case'
    interface ViewState {
      mode: ViewMode
      activeProjectId: string | null
      openCase: (projectId: string) => void
      closeCase: () => void
    }
    ```
    - openCase: MUST check useSliderStore.getState().isAnimating — if true, return early (do nothing). Otherwise set mode='case', activeProjectId=projectId. Per user decision: block case study open while slider is animating to prevent palette bleed.
    - closeCase: set mode='slider', activeProjectId=null

    **Part C: Tests** — Create src/store/__tests__/useViewStore.test.ts:
    - openCase sets mode to 'case' and activeProjectId
    - closeCase resets mode to 'slider' and activeProjectId to null
    - openCase is blocked (no-op) when useSliderStore.isAnimating is true
    - openCase works when useSliderStore.isAnimating is false
  </action>
  <verify>
    <automated>npx vitest run src/store/__tests__/useViewStore.test.ts src/data/__tests__/projects.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 projects have complete caseStudy data in French. useViewStore correctly manages mode and blocks open during animation. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — all tests pass
- `npx tsc --noEmit` — no TypeScript errors
- projects.ts compiles with all 3 projects having caseStudy fields
- useViewStore.openCase is guarded by isAnimating check
</verification>

<success_criteria>
- CaseStudyContent type hierarchy fully defined in types.ts
- All 3 projects have rich, realistic French case study content (no placeholders)
- useViewStore manages slider/case mode switching
- isAnimating guard prevents opening case study during slide transitions
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-scroll-infrastructure-view-scaffold/05-01-SUMMARY.md`
</output>
