---
phase: 02-slider-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/data/projects.ts
  - src/store/useSliderStore.ts
  - src/store/__tests__/useSliderStore.test.ts
  - src/components/slider/Slider.tsx
  - src/components/slider/Slide.tsx
  - src/components/slider/SliderControls.tsx
  - src/App.tsx
autonomous: true
requirements: [NAV-01, NAV-02]

must_haves:
  truths:
    - "User can navigate forward and backward through 12+ project slides with smooth animated transitions"
    - "User can navigate via touch swipe, mouse wheel, pointer drag, and on-screen prev/next buttons"
    - "Rapid input during animation is ignored (no race conditions or overlapping transitions)"
  artifacts:
    - path: "src/store/useSliderStore.ts"
      provides: "Slide state management (currentIndex, direction, isAnimating, navigation actions)"
      exports: ["useSliderStore"]
    - path: "src/components/slider/Slider.tsx"
      provides: "Main slider container with GSAP animation and Observer input handling"
      min_lines: 50
    - path: "src/components/slider/Slide.tsx"
      provides: "Individual slide wrapper rendering ProjectData"
      min_lines: 15
    - path: "src/components/slider/SliderControls.tsx"
      provides: "Prev/next click buttons"
      min_lines: 20
    - path: "src/store/__tests__/useSliderStore.test.ts"
      provides: "Store logic tests for navigation, clamping, animation lock"
      min_lines: 40
  key_links:
    - from: "src/components/slider/Slider.tsx"
      to: "src/store/useSliderStore.ts"
      via: "Zustand useSliderStore subscription for currentIndex and direction"
      pattern: "useSliderStore"
    - from: "src/components/slider/Slider.tsx"
      to: "gsap"
      via: "useGSAP hook with Observer.create for input and gsap.timeline for transitions"
      pattern: "Observer\\.create|gsap\\.timeline"
    - from: "src/App.tsx"
      to: "src/components/slider/Slider.tsx"
      via: "Import and render Slider replacing project grid"
      pattern: "import.*Slider"
---

<objective>
Build the core GSAP-driven project slider with Zustand state management, multi-input navigation (wheel, touch, pointer, click controls), and smooth animated transitions across 12+ projects.

Purpose: This is the central interaction of the portfolio -- replacing the static grid with a full-screen slide-based carousel that demonstrates Hargile's craft through buttery transitions.
Output: Working slider navigable via swipe/wheel/pointer/click with animation lock preventing race conditions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-slider-navigation/02-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Existing types and exports the executor needs -->

From src/data/types.ts:
```typescript
export interface ProjectColors {
  accent: string;
  background: string;
  gradientFrom: string;
  gradientTo: string;
}

export interface ProjectData {
  id: string;
  title1: string;
  title2: string;
  category: string;
  year: string;
  subtitle: string;
  client: string;
  services: string;
  duration: string;
  tech: string;
  heroImg: string;
  colors: ProjectColors;
}
```

From src/data/projects.ts:
```typescript
export const projects: ProjectData[] = [ /* 5 projects: atlas, pulse, verde, lumen, nexo */ ];
```

From src/theme/theme-utils.ts:
```typescript
export interface ThemePalette { accent, bg, bgElevated, bgCard, text, textSecondary, coral, lavender }
export function applyTheme(palette: ThemePalette): void;
```

From src/components/layout/AppShell.tsx:
```typescript
export function AppShell({ children }: { children: ReactNode }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies, expand project data to 12+, and create Zustand slider store with tests</name>
  <files>package.json, src/data/projects.ts, src/store/useSliderStore.ts, src/store/__tests__/useSliderStore.test.ts</files>
  <behavior>
    - nextSlide increments currentIndex by 1 and sets direction to 1
    - prevSlide decrements currentIndex by 1 and sets direction to -1
    - nextSlide does nothing when currentIndex is at last slide (clamped, no wrapping)
    - prevSlide does nothing when currentIndex is 0
    - goToSlide(n) sets currentIndex to n and computes direction from current position
    - goToSlide clamps index to valid range [0, totalSlides-1]
    - All navigation actions are blocked when isAnimating is true
    - setAnimating(false) unlocks navigation
    - totalSlides equals projects.length (12+)
  </behavior>
  <action>
    1. Install gsap, @gsap/react, and zustand:
       `npm install gsap @gsap/react zustand`

    2. Expand src/data/projects.ts from 5 to 12+ projects. Add 7-8 more placeholder projects following the same pattern (unique id, title1/title2, category, year, subtitle in French, client, services, duration, tech, heroImg from Unsplash with w=1600&h=900, defaultColors). Use distinctive project names like: Aura (branding), Forge (industrial app), Prism (data viz), Zenith (architecture site), Echo (music platform), Orbit (space tech), Coral (marine research).

    3. Write tests FIRST in src/store/__tests__/useSliderStore.test.ts. Test all behaviors listed above. Use `import { useSliderStore } from '../../store/useSliderStore'` and test via `useSliderStore.getState()` / `useSliderStore.setState()` (Zustand stores are testable without React rendering). Reset store state between tests.

    4. Create src/store/useSliderStore.ts implementing the SliderState interface per the research RESEARCH.md Pattern 1. Key details:
       - Import projects from '../data/projects' for totalSlides
       - isAnimating lock checked in every navigation action
       - Clamped navigation (no wrapping) per research recommendation
       - direction is 1 (forward) or -1 (backward)

    5. Run tests to confirm green: `npx vitest run src/store/__tests__/useSliderStore.test.ts`
  </action>
  <verify>
    <automated>cd "C:/Argent/Hargile Portfolio/hargile-portfolio" && npx vitest run src/store/__tests__/useSliderStore.test.ts -x</automated>
  </verify>
  <done>gsap, @gsap/react, zustand installed; projects.ts has 12+ entries; useSliderStore passes all tests for navigation logic, clamping, and animation lock</done>
</task>

<task type="auto">
  <name>Task 2: Build Slider, Slide, and SliderControls components with GSAP animation and Observer input</name>
  <files>src/components/slider/Slider.tsx, src/components/slider/Slide.tsx, src/components/slider/SliderControls.tsx, src/App.tsx</files>
  <action>
    1. Create src/components/slider/Slide.tsx:
       - Receives `project: ProjectData` and `index: number` props
       - Renders a full-viewport slide (h-screen w-full) with absolute positioning
       - Displays project title (title1 + title2), category, year, subtitle
       - Shows hero image as background or large visual element
       - Uses `data-index={index}` attribute for GSAP targeting
       - Apply Tailwind classes for typography (font-display for titles, font-mono for year)

    2. Create src/components/slider/SliderControls.tsx:
       - Imports useSliderStore for nextSlide, prevSlide, currentIndex, totalSlides, isAnimating
       - Renders prev/next arrow buttons, fixed-positioned (e.g., left/right sides or bottom corners)
       - Disables prev button at index 0, next button at last index
       - Buttons are visually styled with hover states (opacity/scale transition via Tailwind)
       - Buttons use aria-label="Previous project" / "Next project"
       - Uses clsx for conditional disabled styling

    3. Create src/components/slider/Slider.tsx (the core component):
       - Import gsap, { Observer } from 'gsap/Observer', { useGSAP } from '@gsap/react'
       - Register plugins: gsap.registerPlugin(Observer, useGSAP)
       - Create a containerRef (useRef<HTMLDivElement>)
       - Subscribe to useSliderStore for currentIndex and direction
       - Use useGSAP hook (scoped to containerRef) for:
         a. Initial setup: gsap.set all slides to xPercent: 100, then set first slide to xPercent: 0
         b. Observer.create on the container with type: 'wheel,touch,pointer', wheelSpeed: -1, tolerance: 10, preventDefault: true. onDown calls nextSlide (if not animating), onUp calls prevSlide (if not animating). Access store via useSliderStore.getState() inside Observer callbacks (not React state -- avoids stale closures).
       - Use a useEffect (or useGSAP dependency) watching currentIndex to animate transitions:
         Use contextSafe from useGSAP. Build a gsap.timeline that:
         - Slides the previous active slide out (xPercent: -100 * direction)
         - Slides the new slide in from (xPercent: 100 * direction) to (xPercent: 0)
         - Duration 0.8s, ease 'power3.inOut'
         - onComplete: store.setAnimating(false)
         - Use data-index attributes to target specific slides
       - Render: a full-screen container (h-screen w-screen overflow-hidden relative) containing all Slide components and SliderControls
       - CRITICAL: Use useGSAP hook, NEVER raw useEffect for GSAP code (React 18 strict mode safety per research)

    4. Update src/App.tsx:
       - Replace the Container + heading + project grid with the Slider component
       - Keep AppShell and Navigation wrapping
       - Import Slider from './components/slider/Slider'
       - The slider is the full-page experience now (Navigation overlays on top via z-index)

    5. Verify the dev server runs without errors: `npm run dev` (check console)
    6. Run full test suite to ensure no regressions: `npm run test`
  </action>
  <verify>
    <automated>cd "C:/Argent/Hargile Portfolio/hargile-portfolio" && npm run build && npm run test</automated>
  </verify>
  <done>Full-screen slider renders all 12+ project slides; GSAP Observer handles wheel/touch/pointer input; prev/next buttons work; transitions animate smoothly with 0.8s power3.inOut easing; animation lock prevents race conditions; production build succeeds; all tests pass</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no TypeScript errors
- `npm run test` -- all tests pass (existing + new store tests)
- Dev server shows full-screen slider instead of project grid
- Wheel/swipe/pointer drag navigates between slides
- Prev/next buttons navigate and disable at boundaries
</verification>

<success_criteria>
- 12+ projects render as full-screen slides in a GSAP-animated carousel
- Wheel scroll, touch swipe, pointer drag, and click buttons all navigate between slides
- Transitions are smooth (0.8s power3.inOut) with no jumps or flashes
- Rapid input during animation is safely ignored
- Production build succeeds, all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-slider-navigation/02-01-SUMMARY.md`
</output>
