---
phase: 03-content-theming
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/slider/Slide.tsx
  - src/components/layout/ContactCTA.tsx
  - src/components/layout/AppShell.tsx
  - src/components/layout/Navigation.tsx
  - src/components/slider/__tests__/Slide.test.tsx
  - src/components/layout/__tests__/ContactCTA.test.tsx
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04]

must_haves:
  truths:
    - "Each project slide displays a hero image on the right (desktop) or as full-bleed background (mobile)"
    - "Each project slide shows Problem / Solution / Outcome narrative sections"
    - "Each project displays industry, tech stack items, and year as pill/chip tags"
    - "A fixed CTA button labeled 'Travaillons ensemble' is visible on every slide"
    - "CTA opens a mailto: link"
  artifacts:
    - path: "src/components/slider/Slide.tsx"
      provides: "Split-screen content panel with narrative and metadata tags"
      min_lines: 60
    - path: "src/components/layout/ContactCTA.tsx"
      provides: "Fixed floating mailto CTA button"
      exports: ["ContactCTA"]
    - path: "src/components/slider/__tests__/Slide.test.tsx"
      provides: "Unit tests for Slide content rendering"
    - path: "src/components/layout/__tests__/ContactCTA.test.tsx"
      provides: "Unit tests for ContactCTA rendering"
  key_links:
    - from: "src/components/slider/Slide.tsx"
      to: "src/data/types.ts"
      via: "imports ProjectData with narrative and industry"
      pattern: "import.*ProjectData"
    - from: "src/components/layout/ContactCTA.tsx"
      to: "src/components/layout/AppShell.tsx"
      via: "rendered as child in AppShell"
      pattern: "ContactCTA"
---

<objective>
Restructure the Slide component into a desktop split-screen / mobile overlay layout with rich content (narrative, metadata tags), and add a persistent floating contact CTA.

Purpose: Transforms placeholder slides into story-telling project panels. Each slide communicates what Hargile solved and how, with a clear path to contact.
Output: Restructured Slide.tsx, ContactCTA component, updated AppShell, cleaned Navigation, unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-theming/03-CONTEXT.md
@.planning/phases/03-content-theming/03-RESEARCH.md
@.planning/phases/03-content-theming/03-01-SUMMARY.md

@src/components/slider/Slide.tsx
@src/components/layout/AppShell.tsx
@src/components/layout/Navigation.tsx
@src/data/types.ts

<interfaces>
<!-- Types available after Plan 01 completes -->

From src/data/types.ts (after Plan 01):
```typescript
export interface ProjectNarrative {
  problem: string;
  solution: string;
  outcome: string;
}

export interface ProjectColors {
  accent: string;
  background: string;
  gradientFrom: string;
  gradientTo: string;
}

export interface ProjectData {
  id: string;
  title1: string;
  title2: string;
  category: string;
  year: string;
  subtitle: string;
  client: string;
  services: string;
  duration: string;
  tech: string;
  heroImg: string;
  colors: ProjectColors;
  industry: string;
  narrative: ProjectNarrative;
}
```

From src/components/layout/AppShell.tsx:
```typescript
export function AppShell({ children }: { children: ReactNode }) {
  // Grain overlay + children wrapper
}
```

From src/store/useSliderStore.ts:
```typescript
export const useSliderStore = create<SliderState>(...);
// currentIndex, direction, isAnimating, totalSlides, goToSlide, nextSlide, prevSlide, setAnimating
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restructure Slide.tsx with split layout, narrative, and metadata tags</name>
  <files>src/components/slider/Slide.tsx, src/components/slider/__tests__/Slide.test.tsx</files>
  <behavior>
    - Slide renders an img element with src matching project.heroImg
    - Slide renders three labeled narrative sections: "Probleme", "Solution", "Resultat" with content from project.narrative
    - Slide renders project.industry as a pill/chip tag
    - Slide renders each item from project.tech (split by ", ") as individual pill/chip tags
    - Slide renders project.year as a pill/chip tag
    - Slide preserves data-index attribute on root element
    - All text content elements have data-anim attributes for animation targeting (category, title, narrative, tags, hero)
  </behavior>
  <action>
    Restructure Slide.tsx for desktop split-screen / mobile overlay layout per user decisions:

    Desktop (md: and above):
    - CSS Grid with 2 columns: left content panel (~45%), right hero image (~55%)
    - Left panel: vertically centered, px-16, contains category label, title, narrative sections, metadata tags
    - Right panel: hero image via img tag (not background-image), object-cover, full height

    Mobile (below md:):
    - Hero image as full-bleed absolute background
    - Content overlay with gradient scrim (bg-gradient-to-t from dark) for readability
    - Content at bottom of viewport, stacked vertically

    Content structure (both breakpoints):
    - Category label (project.category) — span with data-anim="category"
    - Title (title1 + title2) — h2 elements with data-anim="title"
    - Narrative section with data-anim="narrative": three labeled blocks:
      - "Probleme" — project.narrative.problem
      - "Solution" — project.narrative.solution
      - "Resultat" — project.narrative.outcome
      Each label uses font-mono text-xs uppercase text-accent, content uses text-text-secondary text-sm
    - Metadata tags with data-anim="tags": render industry, each tech item (split "React, Node.js, AWS" into ["React", "Node.js", "AWS"]), and year as individual pill/chip elements
      Pills: rounded-full px-3 py-1 text-xs bg-accent/10 text-accent border border-accent/20
    - Hero image wrapper with data-anim="hero"
    - Use loading="lazy" on img tags; for index 0 and 1 use loading="eager"

    Remove the old subtitle, client, services, duration display. The narrative and tags replace them.

    Write tests first covering the behavior items above. Use @testing-library/react render + screen queries. Mock a ProjectData object with all new fields.
  </action>
  <verify>
    <automated>npx vitest run src/components/slider/__tests__/Slide.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Slide renders split layout with hero image, three narrative sections, and metadata pill tags; data-anim attributes present on all animatable elements; tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ContactCTA and wire into AppShell; clean Navigation toggle</name>
  <files>src/components/layout/ContactCTA.tsx, src/components/layout/__tests__/ContactCTA.test.tsx, src/components/layout/AppShell.tsx, src/components/layout/Navigation.tsx</files>
  <behavior>
    - ContactCTA renders an anchor element with href="mailto:contact@hargile.com"
    - ContactCTA displays text "Travaillons ensemble"
    - ContactCTA has position fixed, bottom-right corner
    - ContactCTA has z-50 (above slide content, below grain overlay)
  </behavior>
  <action>
    1. Create src/components/layout/ContactCTA.tsx:
       - Fixed position: bottom-8 right-8, z-50
       - Styled as rounded-full pill button with bg-accent text-bg
       - Text: "Travaillons ensemble"
       - href="mailto:contact@hargile.com"
       - Hover: scale-105 + shadow-xl shadow-accent/30 (soft glow effect via accent-colored shadow)
       - Uses Tailwind transition classes for smooth hover
       - Button color adapts automatically via CSS custom properties (bg-accent references --color-accent)

    2. Update src/components/layout/AppShell.tsx:
       - Import and render ContactCTA as a sibling to {children}, outside the Slider
       - Place after {children} but before grain overlay in render order

    3. Update src/components/layout/Navigation.tsx:
       - Remove the theme toggle button entirely (per RESEARCH.md: per-project color morphing replaces manual toggle)
       - Remove useState, applyTheme, defaultTheme, altTheme imports
       - Keep the HARGILE wordmark and nav bar structure

    4. Write tests for ContactCTA first covering behavior items above.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/__tests__/ContactCTA.test.tsx --reporter=verbose</automated>
  </verify>
  <done>ContactCTA renders fixed mailto button with "Travaillons ensemble"; AppShell includes ContactCTA; Navigation has no theme toggle; tests pass</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
npx tsc --noEmit
Dev server renders without errors: npx vite --open (visual check)
</verification>

<success_criteria>
- Each slide displays a hero image, Problem/Solution/Outcome narrative, and metadata pill tags
- Desktop shows split-screen layout (content left, hero right); mobile shows overlay layout
- All animatable elements have data-anim attributes for Plan 03 animation targeting
- Fixed "Travaillons ensemble" CTA button visible at bottom-right on all slides
- CTA opens mailto:contact@hargile.com
- Navigation no longer has theme toggle
- All tests pass, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-theming/03-02-SUMMARY.md`
</output>
