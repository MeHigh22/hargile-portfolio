---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/App.tsx
  - src/components/layout/AppShell.tsx
  - src/components/layout/Navigation.tsx
  - src/components/layout/Container.tsx
  - src/components/projects/ProjectCard.tsx
autonomous: false
requirements:
  - PERF-01

must_haves:
  truths:
    - "Page renders placeholder content for all 5 sample projects"
    - "Layout adapts correctly at 375px mobile viewport"
    - "Layout adapts correctly at 768px tablet viewport"
    - "Layout adapts correctly at 1440px desktop viewport"
    - "Theme toggle switches between two visually distinct color palettes"
    - "Grain overlay effect is visible and matches existing site"
  artifacts:
    - path: "src/components/layout/AppShell.tsx"
      provides: "Full-viewport container with grain overlay"
      exports: ["AppShell"]
      min_lines: 15
    - path: "src/components/layout/Navigation.tsx"
      provides: "Brand mark and theme toggle button"
      exports: ["Navigation"]
    - path: "src/components/layout/Container.tsx"
      provides: "Max-width 1400px container with clamp padding"
      exports: ["Container"]
    - path: "src/components/projects/ProjectCard.tsx"
      provides: "Placeholder project card rendering ProjectData"
      exports: ["ProjectCard"]
    - path: "src/App.tsx"
      provides: "Root layout wiring all components together"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/layout/AppShell.tsx"
      via: "import and render"
      pattern: "import.*AppShell"
    - from: "src/App.tsx"
      to: "src/data/projects.ts"
      via: "import projects array and map to ProjectCards"
      pattern: "projects\\.map"
    - from: "src/components/projects/ProjectCard.tsx"
      to: "src/data/types.ts"
      via: "typed props from ProjectData"
      pattern: "ProjectData"
    - from: "src/components/layout/Navigation.tsx"
      to: "src/theme/theme-utils.ts"
      via: "import applyTheme for toggle button"
      pattern: "applyTheme"
---

<objective>
Build the responsive layout shell with AppShell, Navigation, Container, and ProjectCard components that render all 5 projects and adapt across mobile/tablet/desktop breakpoints. Include a theme toggle button proving the CSS custom property system works.

Purpose: Delivers the visible, responsive skeleton that the slider plugs into in Phase 2.
Output: A running page showing 5 project cards in a responsive grid, with grain overlay, brand navigation, and working theme toggle.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<!-- Depends on Plan 01 outputs -->
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan builds on -->

From src/data/types.ts:
```typescript
export interface ProjectColors {
  accent: string;
  background: string;
  gradientFrom: string;
  gradientTo: string;
}

export interface ProjectData {
  id: string;
  title1: string;
  title2: string;
  category: string;
  year: string;
  subtitle: string;
  client: string;
  services: string;
  duration: string;
  tech: string;
  heroImg: string;
  colors: ProjectColors;
}
```

From src/data/projects.ts:
```typescript
export const projects: ProjectData[];  // 5 entries
```

From src/theme/theme-utils.ts:
```typescript
export interface ThemePalette { accent, bg, bgElevated, text, coral, lavender: string }
export const defaultTheme: ThemePalette;
export const altTheme: ThemePalette;
export function applyTheme(palette: ThemePalette): void;
```

From src/index.css:
- Tailwind CSS 4 @theme with color tokens: bg-bg, bg-bg-elevated, bg-bg-card, bg-surface, text-text, text-text-secondary, text-accent, text-coral, text-lavender
- Font utilities: font-sans (Inter), font-display (Space Grotesk), font-mono (JetBrains Mono)
- Responsive breakpoints: sm:375px, md:768px, lg:1024px, xl:1440px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build responsive layout components and wire App.tsx</name>
  <files>src/components/layout/AppShell.tsx, src/components/layout/Navigation.tsx, src/components/layout/Container.tsx, src/components/projects/ProjectCard.tsx, src/App.tsx</files>
  <action>
1. Create `src/components/layout/AppShell.tsx`:
   - Full-viewport container: `min-h-screen bg-bg text-text font-sans antialiased overflow-x-hidden`
   - Grain overlay as fixed div: `fixed inset-0 pointer-events-none z-[9999] opacity-[0.03]` with the SVG noise filter background-image from the existing site (feTurbulence fractalNoise, baseFrequency 0.9, numOctaves 4, stitchTiles stitch)
   - Accepts `children: React.ReactNode`

2. Create `src/components/layout/Container.tsx`:
   - Max-width 1400px with clamp padding: `mx-auto max-w-[1400px] px-[clamp(20px,4vw,80px)]`
   - Accepts optional `className` prop for composition (use clsx)

3. Create `src/components/layout/Navigation.tsx`:
   - Fixed top navigation bar with brand mark "HARGILE" in font-display
   - Theme toggle button: onClick toggles between defaultTheme and altTheme using applyTheme
   - Use React useState to track current theme state (boolean isAlt)
   - Style: `fixed top-0 left-0 right-0 z-50` with subtle backdrop blur and border-bottom
   - Layout: flex justify-between items-center within a Container
   - Toggle button: small button with text "Toggle Theme" or a sun/moon icon via text character, styled with `text-text-secondary hover:text-accent transition-colors`

4. Create `src/components/projects/ProjectCard.tsx`:
   - Accepts `project: ProjectData` prop (import type from data/types)
   - Renders a card showing: title (title1 + title2), category, year, subtitle, client, tech
   - Shows hero image with `loading="lazy"` attribute, aspect-ratio 16/9, object-cover
   - Card styling: `bg-bg-card rounded-[--radius-lg] border border-border overflow-hidden hover:border-border-hover transition-colors`
   - Title in font-display, category and year in text-text-secondary, subtitle in text-text-secondary text-sm
   - Metadata row at bottom: client + tech in text-text-muted, font-mono, text-xs

5. Update `src/App.tsx`:
   - Import AppShell, Navigation, Container, ProjectCard, and projects array
   - Render: AppShell > Navigation + Container > heading + responsive project grid
   - Heading: "Portfolio" in font-display, text-4xl md:text-5xl xl:text-6xl, text-text, with top padding to clear fixed nav (pt-24 md:pt-32)
   - Project grid: `grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6 md:gap-8 pb-16`
   - Map over projects array to render ProjectCard for each

6. Run `npm run dev` to verify rendering, then `npx vite build` to verify production build.

IMPORTANT design notes:
- Use Tailwind utility classes referencing @theme tokens (bg-bg, text-accent, etc.) -- NOT inline styles or hardcoded hex values
- Use clsx (imported from 'clsx') for conditional classes
- All responsive behavior via Tailwind prefixes (md:, xl:), NO custom media queries
- This is a SKELETON layout -- it will be replaced by the slider in Phase 2. The grid of cards is a placeholder to prove data renders correctly and layout is responsive.
  </action>
  <verify>
    <automated>cd "/c/Argent/Hargile Portfolio/hargile-portfolio" && npx vite build 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1</automated>
  </verify>
  <done>All 5 project cards render in a responsive grid. Navigation shows brand mark and theme toggle. Grain overlay is present. Build succeeds. Existing tests still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify responsive layout and theme toggle</name>
  <files>none</files>
  <action>
Human visually verifies the running application against Phase 1 success criteria. No code changes in this task -- verification only.

What was built:
Responsive layout skeleton with 5 project cards, grain overlay, navigation with theme toggle, and CSS custom property theming across three viewport sizes.

How to verify:
1. Run `npm run dev` in the project root to start the dev server
2. Open the dev server URL (typically http://localhost:5173) in Chrome
3. Verify you see:
   - "HARGILE" brand mark in top navigation
   - "Portfolio" heading
   - 5 project cards with titles (Atlas Analytics, Pulse Health, Verde Market, Lumen Studio, Nexo Finance), categories, hero images, and metadata
   - A subtle grain texture overlay on the background
4. Click the theme toggle button in the navigation:
   - Colors should visibly change (blue accent becomes coral/warm, dark background shifts tone)
   - Click again to revert to the original cool blue theme
5. Open Chrome DevTools (F12) and test responsive viewports:
   - Set to 375px width (mobile): cards should stack in a single column
   - Set to 768px width (tablet): cards should display in a 2-column grid
   - Set to 1440px width (desktop): cards should display in a 3-column grid
6. Verify the grain overlay is visible (very subtle noise pattern on the dark background)
  </action>
  <verify>
    <automated>cd "/c/Argent/Hargile Portfolio/hargile-portfolio" && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <done>User confirms: 5 project cards render correctly, layout is responsive across 3 breakpoints, theme toggle switches palettes, grain overlay visible. Phase 1 success criteria met.</done>
</task>

</tasks>

<verification>
- `npx vite build` succeeds with no errors
- `npx vitest run` -- all existing tests still green
- Visual: 5 project cards render with correct data
- Visual: Grid adapts across 375px / 768px / 1440px viewports
- Visual: Theme toggle switches between cool (blue) and warm (coral) palettes
- Visual: Grain overlay visible on dark background
</verification>

<success_criteria>
- All 4 Phase 1 success criteria are met:
  1. Dev server renders placeholder content for 5 projects (at least 3 required)
  2. Layout adapts across mobile (375px), tablet (768px), desktop (1440px)
  3. CSS custom property theme toggle switches between two palettes
  4. FCP remains fast (no heavy libraries, fonts use display:swap, images lazy-loaded)
- Production build succeeds
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
