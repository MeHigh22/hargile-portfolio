---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/vite-env.d.ts
  - src/data/types.ts
  - src/data/projects.ts
  - src/data/__tests__/projects.test.ts
  - src/theme/theme-utils.ts
  - src/theme/__tests__/theme-utils.test.ts
autonomous: true
requirements:
  - PERF-02

must_haves:
  truths:
    - "Dev server starts and renders a React application"
    - "Project data array contains 5 projects with correct typed fields"
    - "applyTheme function updates CSS custom properties on document root"
    - "CSS @theme variables match the existing site palette exactly"
  artifacts:
    - path: "src/data/types.ts"
      provides: "ProjectData and ProjectColors interfaces"
      exports: ["ProjectData", "ProjectColors"]
    - path: "src/data/projects.ts"
      provides: "Static array of 5 migrated projects"
      exports: ["projects"]
    - path: "src/theme/theme-utils.ts"
      provides: "Theme palette types and applyTheme function"
      exports: ["ThemePalette", "defaultTheme", "altTheme", "applyTheme"]
    - path: "src/index.css"
      provides: "Tailwind import + @theme color tokens + global styles"
      contains: "@theme"
    - path: "vite.config.ts"
      provides: "Vite config with React, Tailwind, and Vitest"
      contains: "tailwindcss"
  key_links:
    - from: "src/data/projects.ts"
      to: "src/data/types.ts"
      via: "import type { ProjectData }"
      pattern: "import.*ProjectData.*from.*types"
    - from: "src/theme/theme-utils.ts"
      to: "document.documentElement"
      via: "style.setProperty for CSS custom properties"
      pattern: "setProperty.*--color"
---

<objective>
Scaffold the Vite + React + TypeScript project with Tailwind CSS 4, migrate the project data model from the existing HTML site, create the CSS theme variable system, and set up the test infrastructure.

Purpose: Establishes the typed foundation that all subsequent plans build on -- data model, theme system, and toolchain.
Output: Running dev server, typed project data, theme utility with unit tests, Tailwind CSS 4 configured with the exact existing color palette.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md

<!-- Source files to migrate from -->
@index.html (lines 16-56 for CSS variables, fonts, selection styles)
@project.html (lines 613-714 for project data model)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold project, configure Tailwind CSS 4, and set up test infrastructure</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, index.html, src/main.tsx, src/App.tsx, src/index.css, src/vite-env.d.ts</files>
  <action>
1. Scaffold the project IN THE CURRENT DIRECTORY (not a subdirectory). Since we already have files here, create the Vite project structure manually:
   - Create `package.json` with name "hargile-showcase", type "module"
   - Create `vite.config.ts` with react and @tailwindcss/vite plugins, plus vitest test config: `test: { globals: true, environment: 'jsdom', setupFiles: [] }`
   - Create `tsconfig.json` and `tsconfig.app.json` matching Vite react-ts template defaults
   - Create `src/vite-env.d.ts` with `/// <reference types="vite/client" />`

2. Run: `npm install react react-dom` and `npm install -D typescript @types/react @types/react-dom @vitejs/plugin-react vite tailwindcss @tailwindcss/vite clsx tailwind-merge vitest @testing-library/react @testing-library/jest-dom jsdom`

3. Create `index.html` (Vite entry point) at project root -- this is the NEW React index.html, separate from the legacy one. IMPORTANT: Move the existing `index.html` to `legacy/index.html` and `project.html` to `legacy/project.html` first so they do not conflict with Vite's index.html.
   - Include Google Fonts link with preconnect (copy exactly from existing site line 7-9)
   - Limit font weights per research: Inter (400,500,600,700), Space Grotesk (500,700), JetBrains Mono (400)
   - Add `font-display=swap` parameter
   - Include `<div id="root"></div>` and `<script type="module" src="/src/main.tsx"></script>`

4. Create `src/index.css` with:
   - `@import "tailwindcss";`
   - `@theme` block containing ALL color tokens from the existing site (copy exact values from index.html lines 16-35):
     --color-bg: #06080d, --color-bg-elevated: #0d1117, --color-bg-card: #111827, --color-surface: #151c2c,
     --color-border: rgba(150,184,247,0.08), --color-border-hover: rgba(150,184,247,0.15),
     --color-text: #edf2fc, --color-text-secondary: rgba(237,242,252,0.55), --color-text-muted: rgba(237,242,252,0.3),
     --color-accent: #96b8f7, --color-accent-dim: rgba(150,184,247,0.12), --color-accent-glow: rgba(150,184,247,0.35),
     --color-coral: #f7a896, --color-coral-dim: rgba(247,168,150,0.12),
     --color-lavender: #b896f7, --color-lavender-dim: rgba(184,150,247,0.12)
   - Radius tokens: --radius-lg: 16px, --radius-md: 8px, --radius-sm: 4px
   - Font families: --font-sans: 'Inter', -apple-system, sans-serif; --font-display: 'Space Grotesk', sans-serif; --font-mono: 'JetBrains Mono', monospace
   - Breakpoint overrides: --breakpoint-sm: 375px, --breakpoint-md: 768px, --breakpoint-lg: 1024px, --breakpoint-xl: 1440px
   - Selection styles: `::selection { background: var(--color-accent); color: #06080d; }`
   - Dynamic theme override variables: `--dynamic-accent: var(--color-accent); --dynamic-bg: var(--color-bg);` (outside @theme, on :root)

5. Create `src/main.tsx` that imports index.css and renders App.
6. Create `src/App.tsx` as a minimal placeholder: renders "Hargile Showcase" text to prove the app works.

7. Verify `npm run dev` starts successfully (Ctrl+C after confirming).

IMPORTANT: Do NOT create tailwind.config.js or postcss.config.js -- Tailwind CSS 4 uses @theme in CSS only.
  </action>
  <behavior>
    - Test: Vite config exports defineConfig with react and tailwindcss plugins
    - Test: Vite config has test block with jsdom environment
  </behavior>
  <verify>
    <automated>cd "/c/Argent/Hargile Portfolio/hargile-portfolio" && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Dev server starts, Vite build succeeds, Tailwind CSS 4 processes @theme tokens, vitest is configured, legacy HTML moved to legacy/ directory</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create typed project data model and theme utility with unit tests</name>
  <files>src/data/types.ts, src/data/projects.ts, src/data/__tests__/projects.test.ts, src/theme/theme-utils.ts, src/theme/__tests__/theme-utils.test.ts</files>
  <behavior>
    - Test: projects array has exactly 5 entries
    - Test: every project has all required fields (id, title1, title2, category, year, subtitle, client, services, duration, tech, heroImg, colors)
    - Test: every project.colors has accent, background, gradientFrom, gradientTo
    - Test: project IDs are unique
    - Test: project IDs match expected set (atlas, pulse, verde, lumen, nexo)
    - Test: applyTheme sets --color-accent on document.documentElement
    - Test: applyTheme sets --color-bg on document.documentElement
    - Test: applyTheme sets all 6 theme properties (accent, bg, bgElevated, text, coral, lavender)
    - Test: defaultTheme has accent '#96b8f7'
    - Test: altTheme has different accent than defaultTheme
  </behavior>
  <action>
1. Create `src/data/types.ts` with:
   ```typescript
   export interface ProjectColors {
     accent: string;
     background: string;
     gradientFrom: string;
     gradientTo: string;
   }

   export interface ProjectData {
     id: string;
     title1: string;
     title2: string;
     category: string;
     year: string;
     subtitle: string;
     client: string;
     services: string;
     duration: string;
     tech: string;
     heroImg: string;
     colors: ProjectColors;
   }
   ```

2. Create `src/data/projects.ts` -- migrate ALL 5 projects from project.html (lines 613-714). Use only the fields defined in ProjectData (not the extended fields like fullImg1, overviewTitle, etc. -- those are Phase 3). Each project gets the default color palette:
   ```typescript
   colors: {
     accent: '#96b8f7',
     background: '#06080d',
     gradientFrom: '#96b8f7',
     gradientTo: '#b896f7',
   }
   ```

3. Create `src/data/__tests__/projects.test.ts` with tests per the behavior block above (RED first, then make GREEN).

4. Create `src/theme/theme-utils.ts` with ThemePalette interface, defaultTheme, altTheme, and applyTheme function -- follow the exact code from the research document's "Theme Toggle Proof-of-Concept" section.

5. Create `src/theme/__tests__/theme-utils.test.ts` with tests per the behavior block above. For DOM tests, mock document.documentElement.style.setProperty.

Run RED-GREEN cycle: write tests first, verify they fail, then create/fix implementations until all tests pass.
  </action>
  <verify>
    <automated>cd "/c/Argent/Hargile Portfolio/hargile-portfolio" && npx vitest run --reporter=verbose 2>&1</automated>
  </verify>
  <done>All unit tests pass. ProjectData interface exported. 5 projects migrated with correct types. applyTheme function correctly sets CSS custom properties. defaultTheme matches existing site palette. altTheme provides a visually distinct alternative.</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- all unit tests green
- `npx vite build` -- production build succeeds with no errors
- `src/data/types.ts` exports ProjectData and ProjectColors
- `src/data/projects.ts` exports array of 5 typed projects
- `src/theme/theme-utils.ts` exports applyTheme, defaultTheme, altTheme
- Legacy HTML files preserved in `legacy/` directory
</verification>

<success_criteria>
- Vite dev server starts and renders the React app
- Vite production build completes without errors
- All vitest unit tests pass (project data integrity + theme utility)
- CSS @theme tokens match the existing site's exact hex/rgba values
- No tailwind.config.js or postcss.config.js exists (Tailwind CSS 4 pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
